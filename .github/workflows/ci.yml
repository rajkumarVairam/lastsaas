name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  tenant-isolation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check tenant isolation
        run: bash scripts/check_tenant_isolation.sh

  model-schemas:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check model schemas and validation tests in sync
        run: bash scripts/check_model_schemas.sh

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"

      - name: Build backend
        working-directory: backend
        run: CGO_ENABLED=0 go build ./...

  test:
    runs-on: ubuntu-latest
    env:
      MONGODB_URI: ${{ secrets.MONGODB_URI }}
      DATABASE_NAME: saasquickstart-test
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"

      - name: Run tests with coverage
        working-directory: backend
        run: |
          CGO_ENABLED=0 go test -p 1 ./... \
            -coverprofile=coverage.out \
            -covermode=atomic \
            -timeout=600s

      - name: Upload coverage to Codecov
        if: always()
        uses: codecov/codecov-action@v5
        with:
          files: backend/coverage.out
          flags: backend
          token: ${{ secrets.CODECOV_TOKEN }}
